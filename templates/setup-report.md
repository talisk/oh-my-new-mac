# 本机配置记录（复制到 .local/ 后填写，不提交实际记录）

- 日期、目标系统/架构、shell：
- 用户原话与动作（检查/配置/继续）：
- 本次工具清单快照 / 分类范围：
- 白名单、黑名单、最终目标集合：
- 冲突策略（ask/auto）与依赖策略（strict/allow-required）：
- 实际增装依赖 / 缺依赖而跳过项：
- 版本、渠道、shell 集成与可选维护偏好：
- 已有授权和无需重复询问的决定：

| 组件 | 原来源/版本 | 保留/新增/修复/跳过及理由 | 改动/备份 | 安装退出码 | shell / GUI / 功能证据 | 待办 |
|---|---|---|---|---|---|---|

## 冲突决定

记录冲突证据、ask 的用户选择或 auto 的解决方式、恢复方法。无法安全处理时说明未满足目标，不能用“跳过”代替尝试可回滚的修复。

## Node / Python（仅选择时填写）

- system Node 完整版本与来源：
- nvm default 完整版本、实际生效管理器与 execPath：
- 数字版本是否完全一致；新 shell / GUI 终端 / 维护任务哪些已验证：
- Python 版本、管理器、解释器路径；uv 的解释器来源与下载策略：
- 保留的旧依赖、未统一项及理由：

## 文件、验证与回滚

按文件记录真实路径、唯一标记、摘要、备份和可回滚差异，不写 API key、OAuth token、cookie 或私有配置正文。区分语法、安装/版本、PATH、交互终端、GUI、认证、实际功能与定时运行；未执行就列原因。

中断后先核对现状再接着做。交接分别列新增、保留、修复、跳过、失败、待用户操作；系统/账号交互不能伪装完成，回滚不覆盖用户后续编辑。
